// #include "../inc/libmx.h" 
// void mx_push_front(t_list **list, void *data)
// {
//     struct t_list *new_node = (t_list *)malloc(sizeof(struct s_list));
//     new_node->data = data;
//     new_node->next = (*list);
//     (*list) = new_node;
// }



// #include "../inc/libmx.h" 
// void *mx_memcpy(void *restrict dst, const void *restrict src, size_t n)
// {
     
    // char *csrc = (char *)src;
    // char *cdest = (char *)dst;
    // size_t i = 0;

    // while(i<n){
    // ++i;
    // cdest[i] = csrc[i];

    // }

    //    for (int i=0; i<n; i++)
    //        cdest[i] = csrc[i];
// }
